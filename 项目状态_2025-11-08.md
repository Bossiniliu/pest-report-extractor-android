# 虫害报告提取器 - 项目状态文档

**更新时间**: 2025-11-08  
**版本**: 2.0  
**状态**: Android 版本存在权限问题，待解决

---

## 📋 项目概述

### 核心功能
这是一个自动化虫害报告数据提取和分析工具，支持桌面版和 Android 移动版。

#### 主要功能列表
1. **PDF 数据提取**
   - 自动识别虫害情况部分
   - 提取建筑物、楼层、部门、点位、虫害类型、数量信息
   - 使用 `pdfplumber` 库解析 PDF

2. **Excel 报告生成**
   - 生成格式化的 Excel 表格
   - 包含数据表和分析报告两个工作表
   - 使用 `openpyxl` 创建带样式的表格

3. **数据分析报告** (v2.0 新增)
   - 数据概览卡片（总记录数、虫害总数、平均密度、最大单点）
   - 虫害类型统计表（含占比）
   - 建筑物虫害统计（含占比）
   - 高危区域 TOP10 排名（前三名高亮）
   - 使用 `pandas` 进行数据统计分析

4. **数据验证**
   - 记录数量验证
   - 字段完整性验证
   - 数据完整性验证（无空值）
   - 逐条数据对比验证
   - 统计信息验证

5. **跨平台支持**
   - macOS / Windows / Linux 桌面版
   - Android 移动版（Kivy 框架）

---

## 🗂️ 代码文件结构

### 桌面版核心文件
```
pest_report_extractor.py        # 主程序（推荐使用）
├── extract_pest_data_from_pdf()    # PDF 数据提取
├── create_pest_excel()             # Excel 生成
├── add_pest_analysis_sheet()       # 分析报告生成
├── verify_pest_data()              # 数据验证
└── main()                          # 主函数（支持 GUI/CLI）
```

**依赖库**:
- `pdfplumber` - PDF 文本提取
- `openpyxl` - Excel 文件操作
- `pandas` - 数据分析
- `tkinter` - GUI 界面（可选）

### Android 版核心文件
```
android_main.py                 # Android 主程序
├── PestReportExtractor         # 核心提取类
│   ├── extract_pest_data_from_pdf()     # PDF 解析
│   ├── create_excel()                   # Excel 生成
│   ├── generate_analysis_report()       # 分析报告
│   └── _draw_*_section()                # 报告绘制方法
└── PestReportApp                # Kivy 应用类
    ├── build()                          # UI 构建
    ├── request_android_permissions()    # 权限请求
    ├── select_pdf()                     # 文件选择
    ├── process_pdf()                    # PDF 处理
    └── extract_and_generate()           # 后台数据处理
```

**Android 依赖**:
- `kivy` - Android UI 框架
- `pyjnius` - Java/Android API 调用
- `android.permissions` - Android 权限管理
- `android.storage` - 存储路径管理

### 存储路径策略
```python
# Android 存储路径配置
try:
    PythonActivity = autoclass('org.kivy.android.PythonActivity')
    Environment = autoclass('android.os.Environment')
    context = PythonActivity.mActivity
    
    # 优先使用 Documents 目录
    documents_dir = Environment.DIRECTORY_DOCUMENTS
    external_files = context.getExternalFilesDir(documents_dir)
    
    if external_files:
        STORAGE_PATH = str(external_files.getAbsolutePath())
        # 路径示例: /Android/data/com.pestcontrol.pestreportextractor/files/Documents/
except Exception as e:
    # Fallback 到内部存储
    STORAGE_PATH = str(app_storage_path())
```

---

## 🚀 GitHub Actions 自动构建

### 仓库信息
- **GitHub 仓库**: https://github.com/Bossiniliu/pest-report-extractor-android
- **分支**: `main`
- **Workflow 文件**: `.github/workflows/build-apk.yml`

### 构建触发条件
1. **Push 到 main 分支** - 自动触发
2. **手动触发** - 通过 GitHub Actions 页面的 "workflow_dispatch"

### 构建流程步骤
```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      1. 📥 检出代码 (actions/checkout@v4)
      2. 🔧 配置 Git 环境
      3. 🐍 设置 Python 环境 (Python 3.11)
      4. 📦 安装 Buildozer 依赖
         - openjdk-17-jdk
         - autoconf, automake, libtool
         - cmake, pkg-config
         - zlib1g-dev, libffi-dev, libssl-dev
         - gettext (提供 autopoint 命令)
      5. 📦 安装 Buildozer (Cython 0.29.33)
      6. 📝 准备主程序 (cp android_main.py main.py)
      7. 🔨 构建 APK (buildozer android debug)
      8. 📊 显示构建结果
      9. 📤 上传 APK (actions/upload-artifact@v4)
      10. 🎉 创建 Release (如果有 tag)
```

### 构建配置 (buildozer.spec)
```ini
[app]
title = 虫害报告提取器
package.name = pestreportextractor
package.domain = com.pestcontrol
version = 2.0

# Android 权限 - 仅使用应用专属存储
android.permissions = INTERNET

# Android API 版本
android.api = 34
android.minapi = 21
android.targetSdkVersion = 34
android.ndk = 25b

# 架构支持
android.archs = arm64-v8a,armeabi-v7a

# 依赖库
requirements = python3,kivy,pyjnius,pdfplumber,openpyxl,pandas,pillow,pycryptodome

# 入口点
android.entrypoint = org.kivy.android.PythonActivity

# 产物类型
android.release_artifact = apk
```

### 构建时间
- **首次构建**: 约 30-40 分钟（需要下载 SDK/NDK）
- **后续构建**: 约 20-30 分钟（使用缓存）

### 下载构建产物
1. 访问: https://github.com/Bossiniliu/pest-report-extractor-android/actions
2. 点击最新的 "Build Android APK" workflow
3. 滚动到底部 "Artifacts" 部分
4. 下载 `pestreportextractor-apk.zip` (约 79 MB)
5. 解压得到 APK 文件

### 最近构建记录
- **Run #14**: 2025-11-08 05:41 UTC - ✅ 成功 (修复 Android 11+ 权限)
- **Run #15**: 2025-11-08 ~08:00 UTC - ✅ 成功 (修复 Android 15+ 闪退尝试)

---

## ⚠️ 当前未解决的问题

### 问题 1: Android 15+ 闪退问题

**测试设备**: 三星 S25 Ultra  
**系统版本**: Android 15 (One UI 8)  
**问题描述**: 
- APK 安装成功，但打开后立即闪退
- 无法进入应用界面
- 无法手动设置权限

**已尝试的解决方案**:

#### 尝试 1: 添加完整存储权限 (失败)
```ini
android.permissions = READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE, INTERNET, MANAGE_EXTERNAL_STORAGE
android.targetSdkVersion = 33
android.manifest.application = requestLegacyExternalStorage="true"
android.manifest.uses-permission = MANAGE_EXTERNAL_STORAGE
```
**结果**: 仍然闪退

#### 尝试 2: 移除所有存储权限 (失败)
```ini
android.permissions = INTERNET
android.targetSdkVersion = 34
# 移除所有 MANAGE_EXTERNAL_STORAGE 相关配置
```
**结果**: 仍然闪退

**权限请求代码简化**:
```python
def request_android_permissions(self, dt):
    """显示存储路径信息（无需权限）"""
    try:
        from android import api_version
        self.update_status(
            f'📂 应用专属存储\n'
            f'📱 Android {api_version} 检测到\n\n'
            f'文件将自动保存到：\n'
            f'{STORAGE_PATH}\n\n'
            f'✅ 无需任何权限！\n'
            f'可以直接使用。'
        )
    except Exception as e:
        self.update_status(
            f'📂 应用专属存储\n'
            f'文件将保存到：\n{STORAGE_PATH}\n\n'
            f'✅ 无需权限，可直接使用。'
        )
```

### 可能的原因分析

1. **Kivy 框架兼容性**
   - Kivy 可能与 Android 15 存在兼容性问题
   - 需要检查 Kivy 版本和依赖

2. **Python 版本问题**
   - 当前使用 Python 3.11
   - Android 15 可能需要特定的 Python 版本

3. **NDK/SDK 版本**
   - 当前使用 NDK 25b
   - Android 15 可能需要更新的工具链

4. **启动时崩溃**
   - 可能在应用启动的早期阶段就崩溃
   - 需要查看 logcat 日志来确定具体原因

5. **依赖库冲突**
   - pdfplumber, openpyxl, pandas 等库可能与 Android 15 不兼容
   - 某些 C 扩展库可能编译失败

### 问题 2: 无法获取崩溃日志

**问题**: 无法通过 ADB 获取应用崩溃日志  
**影响**: 难以定位闪退的具体原因

---

## 🔍 下次调试建议

### 1. 获取详细日志
```bash
# 连接手机，清空日志
adb logcat -c

# 安装并运行应用
adb install -r pestreportextractor-*.apk

# 实时查看日志
adb logcat | grep -i "python\|kivy\|crash\|error\|exception"

# 或保存完整日志
adb logcat > app_crash.log
```

### 2. 创建最小化测试版本
创建一个极简版本的 APK，只包含基本的 Kivy UI，不包含任何业务逻辑：
```python
from kivy.app import App
from kivy.uix.label import Label

class TestApp(App):
    def build(self):
        return Label(text='Hello Android 15!')

if __name__ == '__main__':
    TestApp().run()
```

如果最小版本能运行，说明是业务代码或依赖库的问题。

### 3. 尝试降低 targetSdkVersion
```ini
android.api = 33
android.targetSdkVersion = 30  # 降到 Android 11
```

### 4. 使用官方 Kivy Docker 镜像
```bash
docker run -it --rm \
  -v "$PWD":/app \
  kivy/buildozer:latest \
  bash -c "cd /app && buildozer android debug"
```

### 5. 检查依赖库版本
在 buildozer.spec 中指定具体版本：
```ini
requirements = python3==3.9,kivy==2.2.1,pyjnius==1.4.2,pdfplumber==0.9.0,openpyxl==3.1.0,pandas==2.0.0
```

### 6. 尝试使用 Kivy Launcher 测试
使用 Google Play 的 Kivy Launcher 应用来测试 Python 代码，绕过 APK 打包问题。

### 7. 考虑使用其他框架
如果 Kivy 无法解决，考虑使用：
- **BeeWare** (Python to Android)
- **Chaquopy** (Python in Android Studio)
- **Flutter** + Python backend

---

## 📚 相关文档链接

### 项目文档
- `README.md` - 项目总览
- `Android打包说明.md` - Android APK 打包指南
- `Android权限修复说明.md` - 权限问题修复记录
- `GitHub_Actions_使用指南.md` - CI/CD 配置说明

### 在线资源
- GitHub 仓库: https://github.com/Bossiniliu/pest-report-extractor-android
- GitHub Actions: https://github.com/Bossiniliu/pest-report-extractor-android/actions
- Kivy 文档: https://kivy.org/doc/stable/
- Buildozer 文档: https://buildozer.readthedocs.io/
- Android 存储指南: https://developer.android.com/training/data-storage

---

## 📊 测试矩阵

### 桌面版测试状态
| 平台 | Python 版本 | 测试状态 | 备注 |
|------|------------|---------|------|
| macOS | 3.11 | ✅ 通过 | 完全正常 |
| Windows | - | ⏸️ 未测试 | - |
| Linux | - | ⏸️ 未测试 | - |

### Android 版测试状态
| 设备 | Android 版本 | 测试状态 | 问题描述 |
|------|-------------|---------|---------|
| 三星 S25 Ultra | 15 (One UI 8) | ❌ 失败 | 启动即闪退，无法设置权限 |
| 其他设备 | - | ⏸️ 未测试 | - |

---

## 🎯 下一步计划

### 短期目标
1. ✅ 获取应用崩溃的详细 logcat 日志
2. ✅ 创建最小化测试版本验证 Kivy 兼容性
3. ✅ 测试降低 targetSdkVersion 是否能解决问题
4. ⏸️ 在其他 Android 设备上测试（Android 10-14）

### 中期目标
1. 解决 Android 15 闪退问题
2. 完善错误处理和日志记录
3. 添加应用内的错误报告功能
4. 优化 APK 大小（当前 79 MB）

### 长期目标
1. 支持更多 Android 版本（5.0-15+）
2. 添加云端同步功能
3. 优化 UI/UX 体验
4. 发布到 Google Play Store

---

## 📝 备注

### Git 提交历史
```bash
# 最近的重要提交
0385981 - 修复 Android 15+ 闪退问题 (三星 S25 Ultra) - 2025-11-08
1bcd32e - 修复 Android 权限问题 - 支持 Android 11+ 分区存储 - 2025-11-08
e8e6bdf - (之前的提交) - 2025-11-08
```

### 本地文件
```bash
pwd: /Users/bossiniliu/Documents/Coding/Claude Code/Pest Report Extrator

# 关键文件
android_main.py          # Android 主程序
buildozer.spec           # 打包配置
pest_report_extractor.py # 桌面版主程序

# 文档
Android打包说明.md
Android权限修复说明.md
项目状态_2025-11-08.md  # 本文档
```

### 联系方式
如需继续调试，请提供：
1. 完整的 logcat 日志
2. 应用安装和启动的录屏
3. 手机设置中的应用权限截图
4. 其他测试设备的测试结果

---

**文档结束** - 祝调试顺利！🐛✨
