name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🔧 配置 Git 环境
      run: |
        git config --global --add safe.directory "$GITHUB_WORKSPACE"
        git config --global --add safe.directory '*'
    
    - name: 🐍 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
      env:
        PIP_BREAK_SYSTEM_PACKAGES: 1
    
    - name: 📦 安装 Buildozer 依赖
      run: |
        sudo apt update
        sudo apt install -y \
          git \
          zip \
          unzip \
          openjdk-17-jdk \
          autoconf \
          automake \
          libtool \
          gettext \
          dh-autoreconf \
          pkg-config \
          zlib1g-dev \
          libncurses5-dev \
          libncursesw5-dev \
          cmake \
          libffi-dev \
          libssl-dev \
          libltdl-dev \
          python3-setuptools \
          build-essential
        
        # 验证 autopoint 命令
        echo "=== 验证 autopoint ==="
        if ! command -v autopoint &> /dev/null; then
          echo "autopoint 不存在，查找位置..."
          sudo find /usr -name "autopoint" 2>/dev/null || true
          
          # 尝试重新安装 gettext
          sudo apt remove -y gettext
          sudo apt install -y gettext
          
          # 再次验证
          if ! command -v autopoint &> /dev/null; then
            echo "ERROR: autopoint 仍然不可用！"
            dpkg -L gettext | grep autopoint || true
            exit 1
          fi
        fi
        
        autopoint --version
        echo "✅ autopoint 命令可用"
    
    - name: 📦 安装 Buildozer
      run: |
        pip install --upgrade pip
        pip install cython==0.29.33
        pip install buildozer
    
    - name: 📝 准备主程序
      run: |
        cp android_main.py main.py
        echo "✅ 已创建main.py"
    
    - name: 🔨 构建 APK
      env:
        PIP_BREAK_SYSTEM_PACKAGES: 1
      run: |
        yes | buildozer android debug || buildozer android debug
    
    - name: 📊 显示构建结果
      run: |
        ls -lh bin/ || echo "bin 目录不存在"
        if [ -f bin/*.apk ]; then
          echo "✅ APK 构建成功！"
          ls -lh bin/*.apk
        else
          echo "❌ APK 构建失败"
          exit 1
        fi
    
    - name: 📤 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: pestreportextractor-apk
        path: bin/*.apk
        if-no-files-found: error
    
    - name: 🎉 创建 Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: bin/*.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
